const get_departments = "SELECT * FROM departments";
const get_roles = "SELECT * FROM roles";
const get_employees = "SELECT * FROM employees";

export {
    get_departments,
    get_roles,
    get_employees,
}